---
- name: Variables
  hosts: local
  connection: local
  vars:
    NAME: Mohan
    COMPANY: Accenture
    DURATION: 5
  tasks:
    - name: Print Variables
      debug:
        msg: " {{ NAME }} is working in {{ COMPANY }} from past {{ DURATION }} years"