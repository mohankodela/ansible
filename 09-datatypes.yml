---
- name: datatypes
  hosts: local
  connection: local
  vars:
    User:
      NAME: "Mohan Krishna"
      AGE: 33
    Skills:
      - Apache
      - Linux
      - Jboss
    Experience:
      Apache: 8
      Linux: 5
      Jboss: 7
    tasks:
      - name: pint user details key variables
        debug:
          msg:
            - "Name: {{ NAME }}"
            - "Age: {{AGE }}"
      - name: pint companies list
        debug:
          msg: Skills are {{ Skills }}
      - name: pint Experience mapped to each Skills
        debug:
          msg: Experience in {{ Experience }}