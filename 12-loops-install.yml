- name: loops
  hosts: localhost
  tasks:
    - name: print pesrsons
      ansible.builtin.package:
        name: "{{item}}"
        state: present
      loop:
      - git
      - mysql
      - postfix


